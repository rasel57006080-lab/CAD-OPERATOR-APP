import "./globals.css";

export const metadata = {
  title: "CAD Operator | Portfolio & Tools",
  description: "Professional CAD Operator portfolio and engineering tools"
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
